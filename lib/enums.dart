enum UserTheme {
  system,
  light,
  dark,
}

enum ScheduleType {
  weekly,
  biweekly,
  split,
}

enum NoteType {
  workout,
  exercise,
  other,
  template,
}

enum ImageStorageType {
  local,
  cloud,
}

enum UserImageType {
  progressPic,
}